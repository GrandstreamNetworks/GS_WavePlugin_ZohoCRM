<!doctype html>
<html>

<head>
    <meta charset="utf-8" />
    <title>Zoho CRM</title>
    <script src="<%= context.config.publicPath %>wave-add-in-kit.js" type="text/javascript"></script>
</head>

<body>
    <div id="root"></div>
    <script>
        /**
         * 监听窗口初始化完成
         * 打开默认窗口
         */
        pluginSDK.eventEmitter.once('onInitPluginWindowOk', function ({ pluginPath, preParamObject }) {
            pluginSDK.setDefaultWindow({
                width: 480,
                height: 580,
                center: true,
                show: false,
            })
            console.log('onInitPluginWindowOk', pluginPath, preParamObject);
            sessionStorage.setItem('pluginPath', pluginPath);
            if (preParamObject) sessionStorage.setItem('preParamObject', JSON.stringify(preParamObject))
            window.location.hash = '#/auth'
        })
    </script>
</body>

</html>